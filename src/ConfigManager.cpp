/**
 * @file ConfigManager.cpp
 * @brief Stub implementation for configuration management.
 */

#include "ConfigManager.h"

bool ConfigManager::loadConfig(AppConfig& config) {
    (void)config;
    return false;
}

bool ConfigManager::saveConfig(const AppConfig& config) {
    (void)config;
    return false;
}
